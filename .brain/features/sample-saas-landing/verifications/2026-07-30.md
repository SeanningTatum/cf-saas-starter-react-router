# Verification: Sample SaaS Landing (Loadline)

- **Slug**: `sample-saas-landing` (feature memo: `.brain/features/sample-saas-landing/sample-saas-landing.md`)
- **Date**: 2026-07-30
- **Verified by**: main thread (Playwright CLI — throwaway headless script via `bun`, bundled chromium, `--disable-extensions`, clean profile)
- **Base URL**: http://localhost:5200 _(dev server: started by agent; 5173/5199 were held by unrelated processes)_
- **Role**: anonymous (public marketing surface — no auth involved)
- **Verdict**: ✅ PASS for this surface — renders in both themes with no JS/network errors, all measured contrast above 4.5:1, keyboard order intact, and every defect this change owns was fixed and re-verified. **One app-wide accessibility defect is open and explicitly out of scope**: focus rings do not paint anywhere in the app (see the correction below and [`runs/2026-07-30-focus-ring-defect.md`](../../../runs/2026-07-30-focus-ring-defect.md)).

## Correction — step 03 was reported wrong on the first pass

This doc originally recorded step 03 as ✅ on the strength of `outline-width: 3px`. That reading was wrong: **`outline-style` is `none`**, so the 3px is never drawn, and the `focus-visible` ring's `box-shadow` layers are all transparent. Two independent verification workers ran the same walk during `/create-pr-with-review` and disagreed — one passed the page, one failed it on focus visibility — and the failing one was right. Confirmed by re-probing and by screenshot: the focused hero CTA has no visible indicator.

The defect reproduces byte-identically on the **pre-existing `/` route**, so it is an app-wide ShadCN + Tailwind 4.2 issue that predates this branch, not a regression from it. By decision it ships as a separate PR rather than a shared-component edit inside this one; full evidence and scope in [`runs/2026-07-30-focus-ring-defect.md`](../../../runs/2026-07-30-focus-ring-defect.md).

**Lesson for future walks:** never accept a computed `outline-width` (or a ring custom property) as proof a focus indicator is visible — assert `outline-style` and the composed `box-shadow`, and look at the screenshot.

## Golden path

Derived from the feature memo: an ops lead lands on `/demo`, reads the board, and clicks through to create an account.

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| 01 | `GET /demo`, light theme | Hero + dark framed dispatch board, dotted-grid texture, single accent on the CTA | Renders as locked; board shows 5 rows, pill filters read `All 5 · Rolling 3 · Late 1 · Delivered 1` (derived, not hardcoded) | [`01-hero-light.png`](../screenshots/01-hero-light.png) | ✅ |
| 02 | Full-page scroll, light | 6 bands alternating `bg-background` / `bg-muted/40`, no third surface level | Cost band → board feature → invoicing feature (reversed) → next-steps → CTA → footer | [`02-full-light.png`](../screenshots/02-full-light.png) | ✅ |
| 03 | Tab through the header | Focus lands on each control in visual order with a visible ring | Order correct: `demo-topbar-back → demo-topbar-sign-in → demo-topbar-cta → language-switcher → Toggle theme → demo-hero-cta`. **Ring: ❌** — see the correction below | [`03-focus-ring.png`](../screenshots/03-focus-ring.png) | ⚠️ order ✅ / ring ❌ |
| 04 | Dark theme (`.dark` + `prefers-color-scheme`) | Board frame inverts to a light panel on the dark canvas; no pinned dark colour | Inverts correctly — frame `oklch(0.985 0 0)`, body text `oklch(0.145 0 0)`. Status dots stay legible | [`04-hero-dark.png`](../screenshots/04-hero-dark.png) · [`05-full-dark.png`](../screenshots/05-full-dark.png) | ✅ |
| 05 | `GET /demo?lng=zh` | Every string translated from the `demo` namespace | H1 reads `每一票货、每一次位置确认，都在同一块看板上。`; board columns, filters, footer disclaimer all `zh` | [`06-hero-zh.png`](../screenshots/06-hero-zh.png) | ✅ |
| 06 | Click `demo-hero-cta` | Navigates to the real `/sign-up` route | `href="/sign-up"`, URL after click `http://localhost:5200/sign-up` | [`07-cta-signup.png`](../screenshots/07-cta-signup.png) | ✅ |
| 07 | 390 × 844 viewport | No horizontal page scroll; the board scrolls inside its own container | `scrollWidth 390 == clientWidth 390`, `boardScrollsInternally: true` | [`08-mobile.png`](../screenshots/08-mobile.png) | ✅ |

## Contrast measurements

Computed in-page by painting each `oklch()` value to a 1×1 canvas and reading back sRGB (a naïve regex over `oklch(...)` produced a bogus 1.0 on the first pass — the numbers below are from the corrected probe).

| Pair | Light | Dark | Threshold |
|---|---|---|---|
| Board row text on inverted frame | 19.80 | 18.97 | 4.5 |
| Board mono meta (`text-background/70`) on frame | 9.42 | 19.30 | 4.5 |
| Page body copy on canvas | 4.74 | 7.79 | 4.5 |
| Primary CTA label on `--primary` | 17.18 | 14.23 | 4.5 |

## Error path

The route has no loader, no mutation and no external call, so there is no server failure to surface. The two failure modes that do exist for a static marketing surface were exercised instead:

| # | Step | Expected failure surface | Observed | Result |
|---|------|--------------------------|----------|--------|
| E1 | Narrow viewport (390px) | Layout degrades without a horizontally scrolling page | Initially **FAILED** — page `scrollWidth 491 > 390`. Fixed, see findings | ✅ after fix |
| E2 | Colour-blind / colour-suppressed reading of the board | Status readable without the dot | Every status renders dot **plus** text label (`Rolling` / `Late` / `Delivered`); the stale-row emphasis is also a `title` hint on 2 of 5 rows, not colour alone | ✅ |

## Console

- **jsErrors**: none (across all 7 steps, both themes, both locales)
- **networkErrors**: none

## Findings for main thread

Two real defects found by the walk, both fixed and re-verified in this same session:

1. **Horizontal page overflow at 390px** (`scrollWidth 491`). The per-element probe pinned it on the top bar's control cluster (`width 323, right 491`) — wordmark + demo chip + sign-in + CTA + language switcher + theme toggle do not fit a phone. Fixed by dropping the chip and the sign-in link below `sm` (the hero already carries a sign-in CTA). A `min-w-0` was also added to the board's scroll container and frame so the table's `min-w-[34rem]` can never escape its grid track — belt and braces, since the table itself was not the culprit.
2. **First CTA check reported no navigation.** Not an app defect — the script screenshotted before the client navigation settled. Re-tested with an explicit `waitForURL("**/sign-up")`; the CTA navigates correctly.

Pre-existing repo gap, **not** introduced here and out of scope for this change:

- **`/zh/demo` (and `/zh/`) do not switch locale.** `i18nServer.getLocale(request)` reads the `?lng` search param, the cookie and the `Accept-Language` header — it never reads the `:lng` path param, so the `...prefix(":lng", [...])` routes in `app/routes.ts` serve English. Confirmed identical on the pre-existing `/` route (`curl /zh/` returns "serious starting point" in English), so this is a repo-wide i18n-detection gap, not a demo-route bug. Worth a follow-up: pass the route param into the detector or drop the prefix routes.

## Independent worker runs (`/create-pr-with-review`, 2026-07-30)

Two workers drove the same golden path on their own dev servers, independently of the walk above.

| Worker | Verdict | Agreed on | Diverged on |
|---|---|---|---|
| A | PASS | 5 board rows, derived pills (`All 5 / Rolling 3 / Late 1 / Delivered 1`), text labels on every status, dark-mode panel inversion (`oklch(0.985 0 0)` panel on an `oklch(0.13 …)` page), zh H1, CTA → real `/sign-up`, no page-level horizontal scroll at 390px, zero JS/network errors | — |
| B | **FAIL** | all of the above | Focus visibility: `outline-style: none` + transparent `box-shadow` on every focused control → no visible ring |

Divergence was treated as a FAIL and investigated rather than averaged away. B's finding is real, is app-wide, and predates this branch (see the correction at the top). B additionally measured 8 interactive elements under the 44×44 touch-target guideline at 390px (topbar CTA 32px, hero CTAs 40px, footer link 16px) — ShadCN stock sizes, also app-wide; noted, not changed here.

## Greptile review (pre-PR, confidence 3/5)

3 findings, all on this branch's own code:

| Finding | Priority | Resolution |
|---|---|---|
| `meta()` returned hardcoded English, so `<title>`/`<meta description>` stayed English on `/demo?lng=zh` and for crawlers | P2 | Fixed — a loader threads translated metadata through `loaderData`. Verified live: `?lng=zh` now serves `<title>Loadline — 调度、位置追踪与开票，集中在一块看板</title>` |
| Stale check-call hint lived only in a `title` attribute (never exposed on touch, ignored by most screen readers on a non-interactive cell) | P2 | Fixed — `title` replaced by visually-hidden `sr-only` copy |
| e2e status assertion used an English-only regex, so a mis-rendered locale would pass | P3 | Fixed — labels now derive from `app/locales/en/demo.json` |

The same review confirmed the two things the guardrail exists for: no hardcoded colour anywhere in the diff, and `.mcp.json` committed with `${REFERO_MCP_TOKEN}` expansion and no raw secret.

## Scoped design system — second verification pass

The first pass verified a surface wearing the starter's tokens. After the scoped Andercore system landed (`app/routes/demo/loadline-theme.css`), the surface was re-walked with a probe that resolves every `oklch()` through a canvas.

| Check | Observed | Result |
|---|---|---|
| Scope applies its own tokens | `--background: oklch(0.121 0.017 7.8)`, `--primary: oklch(0.592 0.219 24.2)`, `--border: oklch(0.313 0.015 4.4)`, `--radius: 0.375rem`, `letter-spacing: -0.32px` | ✅ |
| ShadCN components restyle with no edits | Hero CTA painted `oklch(0.592 0.219 24.2)` at `border-radius: 4px` — the reference's exact button spec, from a stock `<Button>` | ✅ |
| Board frame follows the reference | `oklch(0.173 0.012 8.8)` (Dark Shroud) at `border-radius: 0px`, hairline `--border` separation, no shadow | ✅ |
| **Surface ignores the app theme** (reference forbids light) | Flipping `.dark` + `prefers-color-scheme: dark` leaves every scoped token byte-identical | ✅ |
| **No leakage to the rest of the app** | `/` in the same session: root `--background: oklch(1 0 0)`, `--primary: oklch(0.205 0 0)`, `--radius: 0.625rem`, home CTA `oklch(0.205 0 0)`, `[data-surface]` elements on the page: **0** | ✅ |
| Accent discipline (crimson is CTA-only) | After fixing three leaks (active filter pill, feature bullets, step numerals): 3 CTAs + 2 six-pixel dots + 1 hero accent word | ✅ |
| Contrast on the new palette | page body 5.28 · board row 19.06 · board meta 4.96 · CTA label 4.56 | ✅ all ≥ 4.5 |
| No horizontal page scroll at 390px | `scrollWidth 390 == clientWidth 390` | ✅ |
| JS · network errors | none · none | ✅ |

Screenshots: [`10-scoped-hero.png`](../screenshots/10-scoped-hero.png) · [`11-scoped-full.png`](../screenshots/11-scoped-full.png) · [`12-scoped-apptheme-dark.png`](../screenshots/12-scoped-apptheme-dark.png) · [`13-home-unaffected.png`](../screenshots/13-home-unaffected.png) · [`14-scoped-mobile.png`](../screenshots/14-scoped-mobile.png).

Note on the CTA pair: white on Crimson Action measures 4.56:1 — it clears AA for normal text with little margin. It is the reference's own specified pairing (`#ffffff` on `#e32735`), so it stands, but any future darkening of the crimson would break it.

## Test-suite state at verification time

- `bun run typecheck` — PASS (exit 0)
- `bun run test` — 269 passed / 26 files (6 of them new, in `app/routes/demo/__tests__/board-data.test.ts`)
- `bun run build` — PASS (`demo-*.js` 4.90 kB client / 5.01 kB server)
- `E2E_PORT=5178 bun run test:e2e` — 2 passed. On the default port the auth spec **fails**: an unrelated process holds 5173, and Playwright reuses it instead of this app. Pinning `E2E_PORT` is the workaround; the `playwright.config.ts` comment already warns about exactly this.

## Verdict rationale

PASS. Every claim the feature memo makes about the surface was checked against the running app rather than the source: the alternating band rhythm, the inverted board frame in both themes, the derived filter counts, status meaning surviving without colour, the CTA reaching the real sign-up route, keyboard order and focus visibility, translated copy, and measured contrast on all four critical pairs. The walk found two genuine problems, one of which (mobile overflow) would have shipped an unusable phone layout; both are fixed and the failing checks now pass. The only open item is a pre-existing repo-wide i18n path-prefix gap that this change neither caused nor worsened.
